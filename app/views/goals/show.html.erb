カテゴリー：<%= @goal.category.name %>
<br />
やること：<%= @goal.name %>
<br />
<br />


<%= form_with(model: @goal) do |form| %>
    <%= form.hidden_field 'done', value: 'done' %>
    <%= form.submit 'やったボタン' %>
<% end %>

<br />
<br />
